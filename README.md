# Comments-to-Tickets
System collects user comments from different channels. Some comments are just compliments, but others are real issues that should become support tickets. This is a backend application that accepts comments and uses an AI API (Hugging Face) to decide whether a comment should be turned into a ticket, and if so, generate basic structured ticket data.
